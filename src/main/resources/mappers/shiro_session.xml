<?xml version="1.0" encoding="UTF-8" ?>
<shiroSession>

    <updateShiroSession>
        UPDATE t_oauth_session
        SET session_base64 = :sessionBase64, last_visit_date = :lastVisitDate, ip = :ip, update_url = :updateUrl,
            username       = :username
        WHERE session_id = :sessionId
    </updateShiroSession>

</shiroSession>