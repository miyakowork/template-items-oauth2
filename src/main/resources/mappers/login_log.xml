<?xml version="1.0" encoding="UTF-8" ?>
<loginLog>

    <findLogPage>
        SELECT
        toull.*,
        tou.username
        FROM T_OAUTH_USER_LOGIN_LOG toull LEFT JOIN T_OAUTH_USER tou ON tou.ID = toull.USER_ID
    </findLogPage>

    <traceLog>
        INSERT INTO T_OAUTH_USER_LOGIN_LOG (USER_ID, LAST_LOGIN_DATE, LAST_LOGIN_IP) VALUES (?, current_timestamp(), ?)
    </traceLog>
</loginLog>