<?xml version="1.0" encoding="UTF-8" ?>
<user>

    <findUserPage>
        SELECT
        tou.*,
        tod.name AS departmentName,
        tor.cn_name AS roleName
        FROM T_OAUTH_USER tou LEFT JOIN T_OAUTH_DEPARTMENT tod ON tou.dept_id = tod.id
        LEFT JOIN T_OAUTH_ROLE tor
        ON tou.default_role_id = tor.id
    </findUserPage>

    <edit>
        UPDATE t_oauth_user
        SET cname = :cname, email = :email, mobile = :mobile, order_index = :orderIndex, dept_id = :deptId,
        default_role_id = :defaultRoleId, enabled = :enabled, remark = :remark
        WHERE id = :id
    </edit>

    <disabledUser>
        UPDATE T_OAUTH_USER
        SET ENABLED = 0
        WHERE id = :id
    </disabledUser>
</user>